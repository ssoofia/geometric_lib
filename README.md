# Тестирование geometric_lib

## 1. Цели и задачи тестирования

- Подтвердить соответствие математическим формулам и стандартам
- Проверить корректность математических вычислений  площадей и периметров для всех геометрических фигур, присутствующих в проекте geometric_lib
- Протестировать обработку граничных случаев 
- Обеспечить 100% покрытие кода тестами

## 2. Описание тестируемого продукта и область тестирования

**geometric_lib** - математическая библиотека на Python для вычисления площадей и периметров основных геометрических фигур.

### Тестируемые модули:
- `circle.py` - функции area() и perimeter() для работы с кругом
- `rectangle.py` - функции area() и perimeter() для работы с прямоугольником
- `square.py` - функции area() и perimeter() для работы с квадратом
- `triangle.py` - функции area() и perimeter() для работы с треугольником

### Математические формулы

| Фигура | Площадь | Периметр |
|--------|---------|----------|
| Круг | S = pi * r * r | P = 2 * pi * r |
| Прямоугольник | S = a * b | P = 2 * (a + b) |
| Квадрат | S = a * a | P = 4 * a |
| Треугольник | S = 1/2 * a * h | P = a + b + c |

### Коды тестируемых функций 

### circle.py 

```python
import math

def area(r):
    if r <= 0:
        return False
    return math.pi * r * r

def perimeter(r):
    if r <= 0:
        return False
    return 2 * math.pi * r
```

### rectangle.py 

```python
def area(a, b): 
    if a <= 0 or b <= 0:
        return False
    return a * b 

def perimeter(a, b): 
    if a <= 0 or b <= 0:
        return False
    return 2 * (a + b)
```

### square.py 

```python
def area(a):
    if a <= 0:
        return False
    return a * a

def perimeter(a):
    if a <= 0:
        return False
    return 4 * a

```

### triangle.py 

```python
def area(a, h): 
    if a <= 0 or h <= 0:
        return False
    return a * h / 2

def perimeter(a, b, c): 
    if a <= 0 or b <= 0 or c <= 0:
        return False
    if (a + b <= c) or (a + c <= b) or (b + c <= a):
        return False
    return a + b + c

```

## 4. Стратегия тестирования

- **Функциональное тестирование** 

- **Unit-тестирование** 

- **Тестирование граничных значений** 

- **Позитивное тестирование** 

- **Негативное тестирование** 


### Инструменты тестирования:
- **unittest** - фреймворк для модульного тестирования

- **coverage** - инструмент для измерения покрытия кода тестами

## 5. Критерии приемки

### Обязательные условия:
-  Все unit-тесты проходят успешно
-  100% покрытие кода тестами
-  Корректная обработка граничных случаев
- Соответствие математическим стандартам вычислений


### Критерии качества:
- **Точность вычислений** - соответствие математическим формулам
- **Надежность** - стабильная работа при различных входных данных
- **Полнота тестирования** - все функции протестированы 

## 6. Ожидаемые результаты

### Метрики качества:
- **Покрытие тестами**: 100%
- **Успешных тестов**: 100%
- **Неудачных тестов**: 0%



## Запуск тестов

```py
# Запуск всех тестов
python3 -m unittest discover

# Установка библиотеки coverage для измерения покрытия кода тестами
pip install coverage

# Запуск тестов и одновременно измерение покрытия кода тестами
coverage run -m unittest discover

# Просмотр отчета о покрытии кода в терминале.
coverage report
```